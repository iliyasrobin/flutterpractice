import 'package:flutter/cupertino.dart';

class emailFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("emailFrag"),
      ),
    );
  }

}