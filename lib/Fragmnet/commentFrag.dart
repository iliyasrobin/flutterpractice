import 'package:flutter/cupertino.dart';

class commentFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("commentFrag"),
      ),
    );
  }

}