import 'package:flutter/cupertino.dart';

class contactFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("contctFrag"),
      ),
    );
  }

}