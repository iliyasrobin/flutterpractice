import 'package:flutter/cupertino.dart';

class searchFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("searchFrag"),
      ),
    );
  }

}